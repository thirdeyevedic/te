node_modules/
dist/
.astro/
.wrangler/
.dev.vars
.dev.vars.*
__pycache__/
.DS_Store
npm-debug.log*
.env
.env.production

# Project-local assistant state — memory and caches that must never enter the
# deployed repo. Persistent project memory lives at .workbuddy-ai/memory and is
# committed separately when relevant; the rest is local-only.
.workbuddy-ai/
